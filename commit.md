[< к содержанию](/readme.md)

## git commit

**git commit -m *[сообщение коммита]*** - создает новый коммит, сохраняя текущее состояние индекса в локальном репозитории.

Параметр *-m* позволяет написать сообщение из командной строки.

```bash=
git commit -m .
```